name: al_dhiyaa_gallery
description: Al-Dhiyaa Gallery POS and inventory management system.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.0
  mobile_scanner: ^6.0.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
